[package]
name = "binary-reader"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "Endian-aware, seekable binary reader over memory-mapped files. Knows nothing about UI."

[dependencies]
memmap2 = "0.9"
thiserror = "2"

[dev-dependencies]
tempfile = "3"
