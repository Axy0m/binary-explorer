[package]
name = "schema-parser"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "Lexer + parser turning Binary Explorer schema text into a `schema::Schema` AST."

[dependencies]
schema = { path = "../schema" }
thiserror = "2"
